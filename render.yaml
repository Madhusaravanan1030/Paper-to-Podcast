services:
  - type: web
    name: paper-to-podcast
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: GROQ_API_KEY
        sync: false   # Set this manually in the Render dashboard